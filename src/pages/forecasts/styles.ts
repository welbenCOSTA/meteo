import { styled } from "styled-components";

export const Content = styled.div`
  padding: 1rem;
  width: 100%;
  height: 100%;
  background-color: ${props => `${props.theme.colors['gray-500']}60`};
`